// Async Array Mapping:
//Write an asynchronous function that takes an array of numbers and
//returns a new array of Promises where each number is multiplied by
//2 after a delay of 500 milliseconds.

// async function delay(num){
//     return new Promise((resolve) => {
//         setTimeout(() => {
//             resolve(num * 2);
//         }, 500);
//     });
// }

// let arr = [1, 2, 3, 4, 5];
// async function main()
// {
//     for(var i = 0; i < arr.length; i++)
//     {
//         arr[i] = await delay(arr[i]);
//         console.log(arr[i]);
//     }
// }

// main();

// The Asynchronous Shopper:
//Imagine you are building an online shopping application.
//Write an asynchronous function called placeOrder that simulates
//placing an order and returns a promise. The promise should resolve
//with an order confirmation message after a random delay.
